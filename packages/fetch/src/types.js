/**
 * @typedef Remote
 * @prop {string} url Server URL
 * @prop {boolean} insecure If true do not check certificate
 * @prop {string} query Query
 * @prop {boolean} watch If true watch for remote changes
 * @prop {boolean} downloadAll If true download all preview files
 * @prop {boolean} forceDownload If true overwrite existing files
 * @prop {boolean} deleteLocal If true delete local entries which are not present on remote
 */

/**
 * @typedef FetchOption
 * @prop {boolean} [requireEvents] If true the event file must exists
 * @prop {boolean} [query] Query
 */
