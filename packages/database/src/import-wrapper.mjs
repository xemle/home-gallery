import index from './index.js'

export const buildDatabase = index.buildDatabase
export const HeaderType = index.HeaderType
export const isDatabaseTypeCompatible = index.isDatabaseTypeCompatible
export const mergeEntries = index.mergeEntries
export const readDatabase = index.readDatabase
export const readOrCreateDatabase = index.readOrCreateDatabase
export const migrate = index.migrate
export const writeDatabase = index.writeDatabase
export const writeDatabasePlain = index.writeDatabasePlain
